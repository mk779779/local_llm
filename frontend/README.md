# Frontend

Next.js UI for the local LLM backend.

## Setup

```bash
npm install
cp .env.example .env.local
npm run dev
```

By default, the UI expects the backend at `http://127.0.0.1:8000`. Change `LLM_BACKEND_URL` in `.env.local` if needed.
